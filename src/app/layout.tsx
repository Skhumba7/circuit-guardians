import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Circuit Guardians | Your Tech Heroes",
  description: "From broken screens to custom apps. We offer mobile & laptop repairs, software installation, IT support, and full web/app development. Your one-stop tech partner in South Africa.",
  keywords: "tech repair, laptop repair, mobile repair, web development, app development, software installation",
  authors: [{ name: "Circuit Guardians" }],
  icons: {
    icon: '/logo.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
